# Environment Variables
# Copy this file to .env and fill in your values
# .env files are gitignored and should never be committed

# Example variables (customize for your project)
# API_KEY=your-api-key-here
# DATABASE_URL=sqlite://./data.db
# DEBUG=false

# GitHub Token (used by both release-it and electron-updater)
# For release-it: Required permissions: Contents (Read/Write), Releases (Read/Write)
# For electron-updater (private repos): Required permissions: Contents (Read only)
# Note: In production builds, electron-updater loads this from update-config.json instead
# You can use either GITHUB_TOKEN or GH_TOKEN (both are supported)
# GITHUB_TOKEN=your-github-personal-access-token
# GH_TOKEN=your-github-personal-access-token
